# Never commit secrets or session junk
secrets.json
cookies.txt
*_headers.txt
*_response.json
node_modules/
.DS_Store

# gh CLI binary (local tooling only)
gh_*/
ghcli
gh.zip

# exploration leftovers — may contain captured tokens; keep LOCAL only
probe*.mjs
inspect.mjs
group.js
ended.json
.claude/
